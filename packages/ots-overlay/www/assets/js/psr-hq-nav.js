/**
 * ПСС HQ nav — меню «Обращения» вшито в DefaultLayout (боковая панель).
 * Плавающий бар убран. Скрипт оставлен пустым для совместимости с index.html.
 */
(function () {
  var old = document.getElementById('psr-hq-nav-links');
  if (old) old.remove();
})();
