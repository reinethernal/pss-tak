# Gradle
.gradle/
build/
app/build/
local.properties
.kotlin/

# Signing secrets — NEVER commit
keystore.properties
*.jks
*.keystore
*.bks

# Other secrets / certs — NEVER commit
*.p12
*.pem
*.key
*.cer
*.crt
*.pfx
.env
.env.local
.env.*.local

# IDE
.idea/
*.iml
.vscode/

# OS junk
.DS_Store
._*

# Kotlin / Android
*.hprof
captures/

# Build outputs
*.apk
*.aab
*.ap_
*.dex

# Unstripped native libs sourced at build-time for symbol extraction.
# These come from MapLibre's GitHub Releases (see
# playstore-assets/fetch-native-symbols.sh) and weigh ~750 MB total —
# not something to track in source. The stripped versions ship inside
# org.maplibre.gl:android-sdk's AAR; this directory only exists to give
# AGP an unstripped copy so debugSymbolLevel = "FULL" has something to
# extract for Play's native crash symbolication.
app/src/main/jniLibs/

# Agent worktrees
.claude/worktrees/

# F-Droid local/CI secrets and generated repo files (never commit)
fdroid/keystore.jks
fdroid/keystore.p12
fdroid/repo/*
fdroid/repo/.*
!fdroid/repo/icons/
!fdroid/repo/icons/icon.png
fdroid/tmp/

# Firebase / Google API keys (never commit)
**/google-services.json
